const pool = require('../db'); // Assuming you've created a db.js file for pool configuration

const getUsers = async () => {
  // ... your database query logic
};

const createUser = async (user) => {
  // ... your database query logic
};

module.exports = { getUsers, createUser };
